'use strict';

// You can specify certain addons to be allowed
// to have multiple versions here.
// This should only be done for addons that do
// something at build-time.

module.exports = {
  allowedVersions: {
    'ember-decorators': '*',
    '@ember-decorators/utils': '*'
  }
};
